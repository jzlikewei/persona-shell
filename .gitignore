node_modules/
dist/
logs/
*.log

# Sensitive config
bridge/config.yaml

# Runtime
/tmp/
*.pid
*.db
state/

# execution-cron-builder (private, not committed)
.cron/
.ops/

# Deploy config (local only, contains server address)
web-v2/.deploy.env

# MCP config (local only)
.mcp.json

# Outbox (runtime artifacts)
outbox/

# OS
.DS_Store

# Empty dirs (keep with .gitkeep)
config.yaml
im_secret.yaml
